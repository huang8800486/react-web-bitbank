// @font-face {
// font-family: 'PingFang SC-Regular';
// src: url('/@/assets/font/PingFang-SC-Regular.ttf');
// font-display: swap;
// }
html {
  overflow-x: hidden;

  &.hide {
    overflow: hidden;
  }
}

body {
  width: 100%;
  min-width: 320px;
  font-size: 12px;
  overflow-x: hidden;
  color: $base-font-color;
  // background: $base-background-color;
  cs('background', 'base-background-color');
  font-family: Arial, FZShuSong-Z01T, PingFang SC-Regular, PingFang SC, -apple-system, Noto Sans, Helvetica Neue, Helvetica, Nimbus Sans L, Liberation Sans, PingFang SC, Hiragino Sans GB, Noto Sans CJK SC, Source Han Sans SC, Source Han Sans CN, Microsoft YaHei, Wenquanyi Micro Hei, WenQuanYi Zen Hei, ST Heiti, SimHei, WenQuanYi Zen Hei Sharp, sans-serif !important;
}

#app, .container {
  width: 100%;
  position: relative;
  margin: 0 auto;
  overflow: hidden;
  // background: $base-background-color;
}

#app {
  padding-top: constant(safe-area-inset-top);
  padding-top: env(safe-area-inset-top);
  padding-bottom: constant(safe-area-inset-bottom); /* 兼容 IOS<11.2 */
  padding-bottom: env(safe-area-inset-bottom); /* 兼容 IOS>11.2 */
}

.inner_content {
  width: 100%;
  position: relative;
}

.container {
  width: 100%;
  position: relative;
  ms('padding-top', 60px 62px 64px 66px 68px 70px);
}

.wrap {
  width: 860px;
  margin: 0 auto;
}

.clearfix:after {
  content: '\20';
  display: block;
  height: 0;
  visibility: hidden;
  clear: both;
  overflow: hidden;
  font-size: 0;
  line-height: 0;
}

.clearfix {
  zoom: 1;
}

.animate__animated {
  animation-duration: 0.3s;
}

html::-webkit-scrollbar {
  width: 5px;
  height: 5px;
  background-color: #F5F5F5;
}

/* 定义滚动条轨道 内阴影+圆角 */
html::-webkit-scrollbar-track {
  -webkit-box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.3);
  border-radius: 10px;
  background-color: #F5F5F5;
}

/* 定义滑块 内阴影+圆角 */
html::-webkit-scrollbar-thumb {
  border-radius: 10px;
  -webkit-box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.3);
  background-color: #bdbdbd;
}

/* 滑块效果 */
html::-webkit-scrollbar-thumb:hover {
  border-radius: 5px;
  -webkit-box-shadow: inset 0 0 5px rgba(0, 0, 0, 0.2);
  background: rgba(0, 0, 0, 0.4);
}

.htmlScroll::-webkit-scrollbar {
  width: 10px;
  height: 10px;
  background-color: transparent;
}

/* 定义滚动条轨道 内阴影+圆角 */
.htmlScroll::-webkit-scrollbar-track {
  border-radius: 10px;
  cs('background', 'base-filter-scroll-background');
}

/* 定义滑块 内阴影+圆角 */
.htmlScroll::-webkit-scrollbar-thumb {
  border-radius: 10px;
  background-color: #FFDA92;
}

/* 滑块效果 */
.htmlScroll::-webkit-scrollbar-thumb:hover {
  border-radius: 10px;
  -webkit-box-shadow: inset 0 0 10px rgba(0, 0, 0, 0.2);
  background: rgba(0, 0, 0, 0.4);
}

input {
  border: none;
  outline: none;
}

textarea {
  border: none;
  outline: none;
  resize: none;
  overflow: auto;
  border-radius: 0;
}

.fl {
  float: left;
}

.fr {
  float: right;
}

.all_box {
  width: 37px;
  height: 27px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 13px;
  font-family: Arial-Regular, Arial;
  font-weight: 400;
  background: #FFE7B8;
  border-radius: 5px;
  color: #FF8000;
}

html #nprogress .bar {
  ms('height', 2px 3px 4px 6px 8px 8px);
  border-radius: 4px;
  background: rgba(34, 153, 221, 1) !important;
  background: linear-gradient(90deg, rgba(255, 255, 255, 1) 0%, rgba(34, 153, 221, 1) 30%, rgba(34, 153, 221, 1) 100%) !important;
}

:root {
  --popper-theme-background-color: #102648;
  --popper-theme-background-color-hover: #102648;
  --popper-theme-text-color: #ffffff;
  --popper-theme-border-width: 0px;
  --popper-theme-border-style: solid;
  --popper-theme-border-radius: 6px;
  --popper-theme-box-shadow: 0 6px 30px -6px rgba(0, 0, 0, 0.25);
}

.web3modal-modal-lightbox {
  z-index: 1000 !important;
}

.web3modal-modal-container {
  .web3modal-provider-description {
    display: none;
  }

  .web3modal-modal-card {
    width: auto;
    border-radius: 16px;
    grid-template-columns: repeat(2, 1fr);
    padding: 10px;
    ms('min-width', 300px 320px 340px 360px 396px 396px);
    ms('max-height', 60% 60% 100% 100% 100% 100%);

    &::-webkit-scrollbar {
      width: 0 !important;
    }

    &::-webkit-scrollbar {
      width: 0 !important;
      height: 0;
    }
  }

  .web3modal-provider-wrapper {
    border: none;
  }

  .web3modal-provider-name {
    font-size: 16px;
    margin-top: 1em;
  }

  .web3modal-provider-icon {
    width: 32px;
    height: 32px;
  }

  .web3modal-provider-container {
    padding: 16px 16px;
  }
}

#launcher {
  bottom: 50px !important;
}